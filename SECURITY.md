# Security policy

ThermAgent includes privileged host components. Please report security issues privately before public disclosure.

For v0.1, the daemon should only apply allowlisted actions and must keep dry-run and local rollback paths testable.
